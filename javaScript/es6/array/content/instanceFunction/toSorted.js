function test () {
  // 2023
  console.log('---Array.prototype.toSorted()-----------------------------------------------');

};

/**
 * Array.prototype.toSorted() 方法和 sort() 都是将数组排序，但是 toSorted() 不会改变原有的数组
 */

export default test;