function test () {
  // 2023
  console.log('---Array.prototype.toSpliced()-----------------------------------------------');

};

/**
 * Array.prototype.toSpliced() 方法和 splice() 都是删除或替换或增加部分元素
 * 
 * toSpliced()：
 * 不修改原数组
 * 返回的数组是修改后的数组
 * 访问 empty 的时候，会转化成 undefined
 * 
 * splice()：
 * 修改原数组
 * 返回的数组是删除的项
 * 访问 empty 的时候，不会转化成 undefined 处理
 */

export default test;