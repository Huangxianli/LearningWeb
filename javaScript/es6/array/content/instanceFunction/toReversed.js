function test () {
  // 2023
  console.log('---Array.prototype.toReversed()-----------------------------------------------');

};

/**
 * Array.prototype.toReversed() 方法和 reverse() 都是将数组反续
 * toReversed()：
 * 不会修改原数组
 * 返回的一定是一个数组
 * 访问 empty 的时候会转化成 undefined
 * 
 * reverse()：
 * 修改原数组
 * 调用的是什么就返回什么
 * 访问 empty 不会转化成 undefined
 */

export default test;